name: Request

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - master

jobs:
  iss:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - name: pr number
        run: |
          echo "${{env.GITHUB_REF}}"
      # - name: Deploy Stage
      #   uses: fjogeleit/http-request-action@master
      #   with:
      #     url: 'https://cttuyb3bsf.execute-api.ca-central-1.amazonaws.com/test/actions'
      #     method: 'PUT'
      #     data: '{"prNumber": 10, "success": false, "id": 1}'
      #     customHeaders: '{"Authorization": "secret"}'